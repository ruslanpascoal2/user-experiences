import { FilterTasksByColumnPipe } from './filter-tasks-by-column.pipe';

describe('FilterTasksByColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTasksByColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
